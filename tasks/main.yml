---
# tasks file for php5

- include: install.yml
  tags:
    - BARC_INSTALL
    - BARC_INSTALL_PACKAGES

- include: configure-php-cli-sapi.yml
  tags:
    - BARC_CONFIGURE
    - BARC_CONFIGURE_PACKAGE
